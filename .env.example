# The Atlas connection string. Name the database before the '?', or the driver
# quietly uses "test".
MONGODB_URI=mongodb+srv://<user>:<password>@<cluster>.mongodb.net/store?retryWrites=true&w=majority

# trace | debug | info | warn | error | fatal
LOG_LEVEL=debug
